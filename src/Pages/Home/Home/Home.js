import React from 'react';
import Banner from '../Banner/Banner';

const Home = () => {
    return (
        <section className="section-banner">
            <Banner></Banner>
        </section>
    );
};

export default Home;